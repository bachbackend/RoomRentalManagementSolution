@page
@model RoomRentalManagementSolution.Pages.Contract.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>


<p>
    @* <a asp-page="Create">Create New</a> *@
    
        <a asp-page="Create" asp-route-id="@Model.customerId">Create New</a>
    
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Contract[0].DateOfHire)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Contract[0].DateOfExpiration)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Contract[0].Customer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Contract[0].Room)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Contract) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DateOfHire)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateOfExpiration)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Customer.CustomerId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Room.RoomId)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
